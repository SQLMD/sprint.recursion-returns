describe("stringifyJSON", () => {
  describe("basic stuff", () => {
    it("should return an array as string", () => {
      expect("[]").toEqual(stringifyJSON([]));
    });
  });

  // FIXME: Write your tests
  describe("???", () => {
    it("should return the right answer", () => {
      expect(1).toEqual("FIXME: Write tests");
    });
  });
});
